
function authorize(config, credentials, callback) {
    config && config.token && config.token === credentials.token  
    ? callback(null)
    : callback('Invalid token');
}

exports.authorize = authorize;
